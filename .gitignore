#Node modules
node_modules/
dist/

#python
__pycache__/
*.pyc
venv/
env/

#env
*.env
.env

#k8s secrets with real values — template (REPLACE_ME) is safe to commit, filled version is not
k8s/secret.yaml

#os files
.DS_Store

#personal study plan — not for the repo
tasks.md
