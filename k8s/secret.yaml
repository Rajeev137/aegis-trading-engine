# TODO: Create a Secret named "aegis-secrets" in namespace "aegis".
#       type: Opaque
#
# TODO: *** TEMPLATE ONLY — DO NOT COMMIT REAL VALUES ***
#       This file is a scaffold. Replace every placeholder with a real
#       base64-encoded value before applying to any cluster.
#       Encode a value with: echo -n "your-value" | base64
#
# TODO: Secret keys to include (in the `data:` block, base64-encoded):
#
#   DATABASE_URL
#     Full async Postgres connection string:
#     postgresql+asyncpg://aegis_admin:<PASSWORD>@aegis-postgres:5432/aegis_db
#     Must use the K8s service name "aegis-postgres", not localhost.
#     Must match POSTGRES_PASSWORD below.
#
#   SECRET_KEY
#     JWT signing key. Generate a strong one with:
#       python -c "import secrets; print(secrets.token_urlsafe(48))"
#     Never reuse across environments.
#
#   POSTGRES_PASSWORD
#     The Postgres superuser password.
#     Must exactly match the value embedded in DATABASE_URL above.
#     Referenced by statefulset-postgres.yaml as POSTGRES_PASSWORD env var.
#
#   RABBITMQ_DEFAULT_USER
#     RabbitMQ broker username (referenced by deployment-rabbitmq.yaml).
#
#   RABBITMQ_DEFAULT_PASS
#     RabbitMQ broker password (referenced by deployment-rabbitmq.yaml).
#
# TODO: Consider a stringData block alongside data for a human-readable
#       reference template (stringData values are auto-base64'd by K8s but
#       never returned in plaintext from the API).
#
# TODO: In production, do NOT use this file. Use an external secret manager
#       (AWS Secrets Manager + External Secrets Operator, or HashiCorp Vault)
#       and remove this file from the deployment pipeline entirely.
#       This file is for minikube / local development only.
#
# TODO: Add this file to .gitignore once real values are filled in.
#       The template (with placeholder strings) is safe to commit; the filled
#       version with real base64 secrets is not.
#
# See .env.example for the variable descriptions and docker-compose defaults.
